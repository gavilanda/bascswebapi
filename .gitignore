# ---- .NET / build ----
bin/
obj/
publish/
*.user
*.suo
.vs/

# ---- Base de datos local (SQLite de desarrollo) ----
*.db
*.db-shm
*.db-wal

# ---- Caché del padrón (se regenera desde BAS) ----
cache-padron/

# ---- Caché de estadísticas de venta por mes (se regenera desde BAS) ----
cache-ventas/

# ---- Configuración con secretos ----
# El template (appsettings.json) SÍ se sube; la config real de desarrollo NO.
appsettings.Development.json
appsettings.Production.json
appsettings.*.Local.json

# ---- Manuales/recursos pesados de BAS (opcional, ver nota) ----
# BASCS/

# ---- Varios ----
*.log
.DS_Store
Thumbs.db

# Log de la macro de conciliación (salida en runtime)
macro-conciliacion/conciliar.log
